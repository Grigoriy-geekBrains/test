const config = {
    urls: {
        products: '/api/products/get',
        cart: '/api/cart/get',
        addToCart: '/api/cart/add',
        deleteFromCart: '/api/cart/delete',
    }
};

export default config;
